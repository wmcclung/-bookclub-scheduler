DATABASE_URL=postgresql://user:password@localhost:5432/bookclub
NODE_ENV=development
PORT=3000
# Passcode the organizer enters to reach the "My polls" dashboard (create/delete polls).
# Respondents never need this — they just use the poll link you send them.
ADMIN_PASSCODE=change-me
